(function(){

    // DefaultTheme
    // ------------

    // Every theme will inherit the properties of this
    // theme. They can override everything or just a bit
    // of them. Also, every theme can extend any other
    // existing theme.
    Datawrapper.DefaultTheme = function() {};

    _.extend(Datawrapper.DefaultTheme.prototype, Datawrapper.BaseTheme, {



    });

}).call(this);