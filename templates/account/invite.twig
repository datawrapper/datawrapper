{% extends "core.twig" %}
{% set noHeader = true %}
{% set noFooter = true %}
{% block content %}

{{
    svelte('invite', {
        redirect: redirect,
        customLogo: config.custom_logo,
        headline: message_h1,
        intro: message_p,
        button: message_button,
        email: email,
        isPasswordReset: password_reset
    })
}}


<style type="text/css">

    body {
        background: #0f4656;
        background: -moz-linear-gradient(90deg, #0f4656 0%, #085e55 100%);
        background: -webkit-linear-gradient(90deg, #0f4656 0%,#085e55 100%);
        background: linear-gradient(90deg, #0f4656 0%, #085e55 100%);
        filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#0f4656', endColorstr='#085e55',GradientType=1 );
}

</style>

{% endblock %}
