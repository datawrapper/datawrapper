{% extends "core.twig" %}

{% block content %}

    {% if config.custom_home %}

        {% include "custom/" ~ config.custom_home ~ ".twig" %}
        {{ block('home') }}

    {% else %}

        Please add a custom home page.

    {% endif %}

{{ parent() }}

{% endblock %}